# Handover — Parte 3 di 8
> Torna all'indice: [HANDOVER.md](./HANDOVER.md) · [README.md](./README.md)

---

## 11. Change log recente

### Change log — firma co-brand, loop senza stacco, credito «creato con», design system letto dal sito (2026-09-11 → 09-12) → dettaglio §5.9, §8, §13.7, §29.3
- `3e948b0` **fix(core): CI rossa da giorni per una frase ripetuta nell'Admin** — `content-audit` segnalava 10 issue, una per app: era **una sola**, nel motore Admin condiviso, replicata ovunque («Le modifiche si applicano in tempo reale…» identica nel tab soluzioni e in quello capitoli). Differenziato il secondo passaggio. CI **rossa da `ada481f` (11 set)**: nessuno dei commit successivi ne era la causa. Ora `content-audit PASS`.
- `ea82ab2` **fix(showcase): griglie bilanciate, API senza etichette, architettura con la catena di build** — le experience stavano 4+1 su desktop e 2+2+1 su tablet: la causa non è la griglia ma il fatto che **quante card sono visibili lo decide la Console a runtime**, quindi ogni numero fisso di colonne prima o poi lascia una riga orfana. Ora le colonne le sceglie uno script sulle card **davvero visibili** (righe piene, mai una card sola in fondo) + flex `justify-content:center`: misurato a 6 viewport, **ogni riga a 0px di scarto dal centro**. Stesso fix alla riga skin del diagramma (4+2 → 3+3). Tolti i chip «in produzione / credenziale attiva». **L'architettura non mostrava le API**: aggiunta la **catena di build** fra motore e skin (4 comandi → token+asset+provenance ↓); chip del motore aggiornati (co-brand, credito, loop video). Nel blueprint **mancava del tutto** la sezione sulla catena di build: aggiunta.
- `25fcfd6` **feat: il design system del cliente si legge, non si ricorda** — `pnpm brand:tokens <url>` (`scripts/brand-tokens.ts`) scarica il CSS di produzione e conta: su UniCredit **petrolio 271 occorrenze contro rosso 13**, e trova `unicredit-regular` (proprietario → non distribuibile). Riproduce da solo una correzione che era costata una sessione. Separa i default di framework, mostra le custom property del sito, elenca i caratteri. **Primo comando di ogni nuova experience** (CLAUDE.md + skill design + skill intake, che ora chiede l'URL del sito). Showcase: nuovo passo 04 «Leggi il brand» nel flusso + sezione dedicata nel blueprint; blocco API da 3 a **9** voci con l'uso concreto di ognuna. Corretta la rivendicazione **C2PA**: i file spediti sono ri-ritagliati e riconvertiti, **non portano più il manifest** — la nota lo dice e rimanda alla provenance.
- `3a47caa` **feat(unicredit): l'ultima slide si firma con la luce** — chiusura e slide-firma usavano lo stesso stagger: sull'ultima è un'eco, non un'animazione. Un fascio ciano attraversa la colonna **una volta sola** e dove passa la firma compare; poi il marchio Adobe si accende e si tira la riga. Maschera e fascio guidati dalla **stessa** animazione di posizione. Tre gotcha: `deck:change` scatta all'**inizio** della transizione (~500ms) → la firma si scriveva fuori campo; il contenuto resta **visibile di default** (verificato con reduced-motion **e con JS disattivato**); un decoro che deborda in orizzontale allarga lo `scrollWidth` e fa fallire il check `c`.
- `bdd39fe` **feat(unicredit): il ritratto esce dallo stock, e il rosso torna a essere il marchio** — il ritratto di Marco era una foto stock in un parco: l'unica immagine stock **davvero visibile** (le altre 8 stanno sotto scrim all'85-90%). Scontornato con la **Remove Background API** e appoggiato su una banda petrolio; registrato in provenance come derivato con licenza dell'originale; la slide dichiara `data-made-with="Photoshop"`. Tolte **6 righe duplicate** «Scena generata con Adobe Firefly» (una per slide-story) che il credito per slide ora fa meglio. **4 sfondi** in `coworker.astro` stavano dentro `.slide-inner` invece che nello slot `backdrop`. Le barre dei pesi per canale erano **rosse**: il rosso è il marchio, il peso di un canale è un dato neutro → petrolio.
- `ada481f` **chore: versiona le critique di design, ignora lo stato per-macchina** — `.impeccable/hook.cache.json` in `.gitignore` (percorsi assoluti, ID sessione); `critique/*.md` versionate perché `/impeccable polish` le rilegge. Verificato prima che non contenessero riferimenti riservati.
- `d1aa12c` **feat: il credito «creato con», e l'allineamento diventa un check** — `@edf/core/blocks/MadeWith.astro`: i prodotti Adobe usati per **creare** gli asset della slide (non quelli che l'experience propone), in basso a destra, speculare alla firma; runtime in `DeckContainer` → ogni deck futuro lo eredita. **Il credito è un fatto**: isybank e MIM tutti Firefly → una riga nel BaseLayout; UniCredit **asset misti** → nessun fallback, marcate a mano le 13 slide con asset `ff-*` o clip. Nuovo check **HARD `m`** dell'audit: i blocchi a bandiera devono condividere il bordo sinistro (esclude decorazione fuori flusso e blocchi senza testo). Tolto «Visual · Adobe Firefly» dalla nav isybank: la nav è fatta di destinazioni.
- `8ef75f4` **feat(isybank): il filo dei momenti, e le sei idee liberate dalle card** — `IsyThread.astro`, un solo SVG montato con **`transition:persist`**: la navigazione SPA non lo ricrea, quindi fra capitoli **non riparte, scorre**; vive nella fascia sotto `--deck-chrome-safe` → non può coprire testo (54px di margine minimo misurato). Le sei idee da sei volte lo stesso template a due card a composizioni asimmetriche col passaggio «oggi → il momento giusto». **Difetto vero trovato**: il div del poster video aveva `class="absolute inset-0"` e la regola globale `[data-slide]:has(.im-video) > .inset-0 { z-index:2 }` lo mandava **sopra gli scrim** → nessuno scrim funzionava, note di copertina a **1,29:1**; migrando a `LoopVideo` condiviso **10,5:1**. Audit soft da 26 a 16, 0 HARD.
- `aece6f6` **feat: la firma Adobe × Brand su tutte le experience, e un gate di audit che funziona** — rollout su UniCredit, Ferrari, Agos, Eni, Isybank, MIM; ordine raddrizzato ovunque (nav, title, description, admin, manifest) tranne «U.S. Bank × Adobe», che è un caso cliente. **L'ultima slide diventa una slide-firma a sé** (la chiusura densa non regge il lockup grande). **Inchiostro**: `--ink-X` sta su `--surface-X`, ma quale sia scura **cambia** — nelle dark-dominant `--surface-inverse` è la superficie CHIARA. ⚠️ **Gate rotto scoperto qui**: `audit:deck --only a,b` finiva in un secondo filtro posizionale che lo leggeva come il nome di rotta `"a,b"` → **zero rotte auditate e «PASS — all decks clean» stampato lo stesso**. Corretto; zero rotte ora esce con codice 2. Con il gate riparato **nessun deck è a zero**: il residuo è tutto soft (`a`, `i`), gli HARD sono a zero sulle rotte toccate.
- `5d58625` **feat(core): la firma Adobe × Brand e i loop senza stacco diventano regola del motore** — `CoBrand.astro` con l'ordine **non invertibile** scritto nel markup; `chrome` su ogni slide, `hero` su prima e ultima; runtime in `DeckContainer`. `LoopVideo.astro` + **`pnpm loop:seamless`**: lo stacco sul giro è un difetto del **contenuto**, non della riproduzione — lo script dissolve la coda sulla testa e **misura** (PSNR giro vs fotogrammi adiacenti), `--check` esce ≠0.
- `901ca12` **feat(unicredit): marchi al centro in copertina, clip in loop senza stacco** — i due marchi grandi al posto dell'occhiello di testo; clip ricucite (cover **21,5→41,1 dB**, chiusura **18,7→34,8 dB**); `preload="auto"`. Residuo noto e accettato: il `loop` nativo costa **un tick di compositor (~17ms, un fotogramma)** sul giro.


### Change log — UniCredit design system vero + gating capitoli/slide + showcase riallineato (2026-09-10) → dettaglio §5.8, §7, §13.6
- `704b888` **feat(unicredit): loghi Adobe × UniCredit su ogni slide, chiusura come capitolo a sé** — firma coi loghi (Adobe SVG inline + file UniCredit ufficiale monocromo a `currentColor`) montata una volta nel BaseLayout → su ogni slide; versione grande sulla chiusura al posto della vecchia firma testuale, che era **l'ultima occorrenza invertita** «UniCredit × Adobe» (spezzata tra `<span>`, invisibile a un grep normale). Slide di chiusura estratta in **`/chiusura/`**, capitolo proprio: nav, `SECTION_FLOW`, `PAGE_REGISTRY`, `ROUTE_SETS` aggiornati insieme. Ripristinati **verbatim** `marcoSteps`/`demoOrder`, che erano stati riscritti a memoria.
- `26252df` **feat(showcase): blueprint riallineato al reale, toggle di pubblicazione, sezioni Firefly e dossier** — il blueprint elencava **3 app su 12** e ignorava `factory-hub` (la radice); «audit a 12 punti, check a–l» **non è mai esistito** (nel codice: `a b c d e g h i j k` + `exp` = **11**, nessun `f`, nessun `l`) → corretti blueprint, `CLAUDE.md` e l'header dello script. Registry showcase da 5 a **10** experience con capitoli contati sulle rotte reali; **toggle di pubblicazione** in Super Admin (`show_in_showcase`, migrazione `0013` applicata al DB remoto, **policy anon solo sulle righe pubblicate** — da anon la select ne restituisce 5 su 10) e runtime **fail-closed**; sezioni **Firefly** (API per nome) e **dossier** (solo metodo, nessun link né nome cliente). Screenshot rifatti per le 4 nuove + UniCredit.
- `77e299c` **feat(core): Admin Console — accendere e spegnere capitoli e singole slide** — quarto tab; runtime in `DeckContainer` (non nei BaseLayout) → vale per ogni experience presente e futura senza wiring. Vedi §7.
- `0774017` **feat(unicredit): asset rigenerati sulla palette vera e inchiostri che si ribaltano** — 5 slot Firefly + 2 clip rigenerati sul petrolio; `duotone` per-slot (era cablato sui colori Max Mara nello script condiviso); `VideoSlot.scrub:false` (6 MB → 1,5 MB); **140 blocchi di testo sotto AA → 0 su 1099**, con i token di inchiostro che si ribaltano con la superficie. Clip caricate sulla Release `media`.
- `924ca20` **feat(unicredit): design system riallineato a quello vero di UniCredit** — petrolio `#007A91` primario, rosso `#E2001A` di marchio, Manrope+Inter; letti dal CSS di produzione del sito, non a memoria. `audit:deck` HARD **da 11 a 1**. Trovato e corretto `.uc-panel` fuori da `@layer` che batteva l'utility `flex-row` (slide-persona sforava di ~700px). `deck-audit`: attesa di `document.fonts.ready` (misurare prima dei webfont rendeva l'esito **non deterministico**) e flag `--only <rotta>`.
- `60bfe74` **feat(unicredit): slide 05 dello scenario — due schermate mostrano il flusso ChatGPT → unicredit.it** — mockup CSS coi token del deck; contrasto misurato sui pixel (min 5,5:1); compact mode sotto i 960px d'altezza che comprime **solo il bianco, mai il tipo**.
- `c507467` **chore(unicredit): lockup «Adobe × UniCredit»** — 4 occorrenze invertite + rinomina del dossier riservato.

### Change log — Isybank: deck PowerPoint su template Adobe per l'incontro Valitutti, v1→v3 (2026-09-09 sera) → dettaglio §31.5 (HANDOVER-06)
- Nessun commit (tutto in `docs/Intesa Sanpaolo/output/`, git-ignored). **v1** generata da zero con python-pptx sul master Adobe Template 2026 (10+4 slide, claim audit 39 claim, registro fonti); **v2** sulla bozza dell'utente (clip Firefly in cover, zero riferimenti al dossier, POV tradotto, TSB/BBVA nel template POV); **v3** (cover immagine Firefly, slide 2 numeri grandi, slide 6-8 rifatte in italiano nello scenario Isybank con 12 immagini Firefly, divisorio referenze, loghi U.S. Bancorp/J.P. Morgan). Memoria `isybank-momento-valitutti` aggiornata (regole TSB/BBVA, leak-check dossier).

### Change log — UniCredit: il viaggio intero di Marco + «Analizza» ricucita sull'attribution (2026-09-09 sera) → dettaglio §5.7 (HANDOVER-01)
- (segue, tarda sera) **feat(unicredit): passata completa sul deck su richiesta slide-per-slide** — home cover con **clip Firefly Video** in loop (non statica); cover di **tutti** i capitoli uniformi (`UcCover`: rail capitoli + chip «in questo capitolo»), Scenario cover con skyline Firefly; gap a 4 card uguali; **momento GenStudio** aggiunto (6 momenti); engine-map con card uniformi; slide promessa rimossa; **chiusura** nuova in Risultati con clip Firefly; copy «intero» corretto; Coworker cover come ponte da Analizza. `audit:deck` 0 HARD su home+6 sezioni a 3 viewport. **Secondo giro (feedback su 4 screenshot)**: engine-map ridisegnata a 6 tessere pulite (numero, nome, prodotti, pallino oro/rosso «lo vive Marco / lo vede il team»), clip video ricodificati **ping-pong** (loop continuo, 10 s, ~1 MB) con credit Firefly in home e sulle slide-momento, stepper `.uc-steps` a una riga (persona + chiusura), chiusura con «Il viaggio di Marco» sopra i 6 motori.
- (segue, stessa sera) **feat(unicredit): la storia di Marco come flusso di 5 slide-momento + 5 backdrop Firefly** — l'utente ha bocciato il pannello unico («è terribile: deve essere un flusso di racconto»). Risposte alle 3 domande: 5 momenti (uno per capitolo) · scene Firefly senza volti + mock UI · sfondi statici. Scenario da 6 a **11 slide** (`slide-story-01…05` tra persona ed engine-map); template testo-sinistra/mock-destra, `marcoMoments[]`, stili `.uc-story-*`/`.uc-mock-*`; slot `ff-story-01…05` in manifest + provenance mergiata. `audit:deck` Scenario **0 HARD** a 3 viewport; 7 slide lette a 1920 IT+EN.
- `3c1a555` **feat(unicredit)** — richiesta utente slide-per-slide: (1) **Scenario 4/6 (Marco)** senza pill prodotto, racconta l'**intero flusso** in 5 beat = i 5 capitoli (`marcoJourney`, stepper `.uc-journey`); (2) **Scenario 5/6 (engine map)** ogni motore ha la riga «**Per Marco**», i motori 05/06 anche «**Dentro UniCredit**» (marketer/analista/consulente); (3) slide **AI Agents in AEM** spostata da Contenuti a **Il Sito** dopo Edge Delivery (coerente con la mappa motori: engine 01); (4) **Sofia rimossa** da Contenuti (quote GenStudio e vincolo «varianti» ora su Marco); (5) **Analizza riscritta come un solo racconto** «Chi ha portato Marco al mutuo?» con l'attribution come filo: nuova `slide-journey` (CJA cuce 8 touchpoint), poi lasttouch → agent → llm (fusa con la vecchia llm-mix) → costpersale → campaign → cxa-brand («Tutto questo è Adobe CX Analytics») → everywhere con CTA al Coworker. **Rimosse** `slide-banking` e `slide-llm-mix`. `PAGE_REGISTRY`, home e `appearsIn` aggiornati. `audit:deck` **0 HARD** su Scenario/Il Sito/Contenuti/Analizza a 1920/1440/1280 (bonificati anche i residui 1280 pre-esistenti su aem-agents/brand-visibility/brand-concierge e il contrasto dei link fonte su slide-data); 16 slide lette a 1920 in IT e EN.
- Assunzione dichiarata: la numerazione dell'utente («3 di 4», «4 di 5») corrisponde a una config soluzioni salvata che nasconde le slide gated Brand Visibility/Brand Concierge; le modifiche sono sui file e valgono con qualunque gating.

### Change log — Isybank: fonti verificate + copy «da costruire insieme» + sfondi/video Adobe Firefly (2026-09-09 pom.) → dettaglio §31.4 (HANDOVER-06)
- `94e5a57` Fonti online verificate e citate su ogni slide (2 correzioni di fatto: iF Award = ecosistema digitale; nuovi clienti 900k/150k al posto di 480k/350k); slide richieste riscritta come 4 domande aperte; 17 backdrop + 2 clip Firefly (Release `media`) con credit esplicito; `audit:deck` 0 HARD, 17 slide lette a 1920.
- `cea02f9` **fix poster dei clip Firefly (Isybank + MIM)**: `href()` forza la slash finale → `x.jpg/` = **404 su Pages** (i poster MIM erano rotti dal 8 set senza che si notasse perché il clip parte comunque). Path dei file in `public/media` ora costruito da `BASE_URL` senza slash; verificato 200 sul live.

### Change log — Isybank «Il momento giusto»: nuova esperienza + dossier war-room per l'AD Valitutti (2026-09-09) → dettaglio §31 (HANDOVER-06)
- `113c579` `apps/isybank-momento` nuova (deck IT 3 capitoli + 6 idee gated, design `.im-*`, Manrope+Inter); registrata in deploy/hub/audit/root; `docs/Intesa Sanpaolo/` git-ignored (unico posto con dati contrattuali). `audit:deck` 0 HARD. Memoria `isybank-momento-valitutti`.
- `0ce4287` + `7b8bbc0` **dossier gated come MIM** (utente: «come sempre, fallo sempre»): contenuto in Supabase `restricted_docs` (`isybank-valitutti`), login/RLS o secret-link `?t=`, nulla nel bundle; `0012_seed_isybank.sql` (registry) + README per il seed privato; DB remoto seedato, token letto. **Regola resa BINDING** in memoria `dossier-as-internal-web-page` (pattern inline Eni/FST = superato).

### Change log — MIM «La voce del Ministero»: evoluzione UI/UX post-critique + Firefly Video sbloccato (2026-09-09, dal più recente) → dettaglio §28.7, §29
- `00147ab` **feat(mim): phase 3** — bridge presenter nel layout, registry admin/presenter sui 18 id, contratto video nel CSS (scrim senza immagine/colore sopra il clip, contenuto sollevato solo con z-index — un `position:relative` sul wrapper assoluto aveva prodotto 3 HARD, corretti), resync fade-scroll a 1,4 s, quote lh 1.45. Build 12 app, typecheck/lint 0, audit 0 HARD, 18 slide lette a 1920+1366.
- `87f83a7` (merge P2a–P2e) **pagine**: `index` (cover video+tricolore, tesi+3 verbi), `domanda` (processi · **`slide-scala`** · domande a passi), `storia` (**ponte video** · ritratto · **notte a 3 battute** · evolve), `voce` (ha già/manca · **`slide-unica`**), `competenze` (**journey builder** + simulatore), `accesso` (link diretti ACN/Consip · garanzie a passi), `persone` (5 misure · schede-dato), `rotta` (5° passo DGSIS · chiusura video).
- `87f83a7` **presenter**: `/presenter/` + `PresenterBridge` + `presenter.config.ts` + note relatore in `src/data/notes/*.ts`.
- `8546677` (merge Fase 1, `c8f4ca0`) **infra**: core sub-step/`deck:change`/`data-on-dark`/tier 1366/`aria-live`; layout `<title>` nuovo + OG + `<main>` + skip-link + leggibilità + `SECTION_FLOW` (storia prima di voce) + `preloadImage`; nav riordinata + `<select>` phone + «Aa» + «?»; `global.css` «Repubblica, non crema» (token blu Italia, card piatte, `.alf-src` ≥4,5:1, classi chart/source-card/phone/sim/video/legibility). Parità audit unicredit 155=155.
- `0d8ae54`+`fdd557e` **asset**: 3 clip Firefly Video sul Release `media` + poster; 8 backdrop rigenerati (texture carta/moduli/inchiostro) e 23 ricompressi 9,1→4,4 MB; poster flythrough orfano rimosso.
- `d50c527` **fix(firefly): Video API sbloccata** — serviva solo `x-model-version: video1_standard` (non l'entitlement enterprise: chiude il P2 «Firefly Generate Video API»). `cc8d560` ordine route audit.
- Critique MIM (8 set, sera): 25/40, 3 P1 (contrasto fonti navy, 1366×768, peso immagini) + P0 contatore/`<title>` dalla design review; snapshot `.impeccable/critique/` (non tracciato).

### Change log — Aperture: osservatorio email tracking come Experience Design pubblico (2026-09-08) → dettaglio §30
- **feat(aperture): nuova app `apps/aperture-email`** — deck di ricerca individuale (Antonio Gargiulo, «lavoro in Adobe»), skin *Aperture* (grafite `#12151C` / avorio `#F3EFE6` / verde segnale `#2EE6A6` / ambra `#FFB547`; Space Grotesk + Inter), clonata dallo scheletro Eni Orbita (BaseLayout runtime completo, Admin Console, gating). 7 pagine: home (cover · tre date · percorso 2×3) · metodo · numeri · pattern · prima-dopo · banche (gate `fsi`) · implicazioni (gate `design`). Contenuti dall'analisi mbox (`garante_analysis/`, fuori repo) + ricerca web (Garante provv. 284/2026 e comunicato 21 apr, CNIL 14 apr 2026, EDPB 2/2023 v2, Google sender guidelines, RFC 8058, Litmus). Regole editoriali: fonti citate **senza interpretazione** delle posizioni Garante/CNIL; brand nominati (scelta dell'autore) tranne un caso di credito al consumo cliente Adobe tenuto anonimo; nessun dato personale. `audit:deck`: **0 hard** a 1920/1440/1280 (residui solo `a`/`i` su cover ariose e slide dense, per regola non si riduce il type); screenshot 1920 letti. Registrata in `deploy.yml` (merge+verify), hub, `deck-audit.ts` ROUTE_SETS.

### Change log — Experience Atelier: pass «critica iper-critica» (2026-09-08) → dettaglio §21.6
- `3e74503`+`fd62926` **fix(atelier): critique pass** — su richiesta «valuta in maniera iper critica… procedi su tutto». Critique a 3 agenti (23/40, 3 P0) → 5 worktree paralleli per area di file + merge + fix di coordinamento. **Ridatato** il piano (M1 metà dic 2026 · M2 Summit 22–25 mar 2027 · M3 fine giu 2027; decisione entro 30 set), **7 experience** con **muro a screenshot reali**, **decisione esplicita** senza cifre (Annex A), `slide-moments` rimossa, **Gantt vero** + **KPI numerici** (`slide-kpi` + `slide-kpi-2`), **sponsor cut `?s=asks`** (14 slide, solution id `detail`, `?s=` pinnato in sessionStorage), layout system `.atl-*`, core a11y (html lang, aria-pressed, label chrome localizzate, lingua che sopravvive alla nav SPA), nav mobile `<select>`, copy de-AI + FR senza franglais. Build tutte le app, audit 0 HARD, screenshot letti. Memoria `experience-atelier-deck` aggiornata.

### Change log — FSTechnology Adobe Day: dossier war-room + skill «dossier come pagina web interna» (2026-09-08) → dettaglio §26.8, §13.4
- `1e7079d` **feat(dossier): FSTechnology Adobe Day war-room + skill «dossier come pagina web interna»** — nuova opportunità Gruppo FS (mail Dario 8 set): Adobe Day con **Maurizio Giampaolo** (DBP Trasporto FSTechnology). (1) **Nuova pagina `/dossier/`** in `apps/trenitalia-connessioni` (orfana, `noindex`): war-room di prep — executive summary, mission brief, chi-è, perimetro societario FS (card Passeggeri/Merci), fatti verificati (badge, incl. warning competitivo **Salesforce**), big idea (Passeggeri + Merci/B2B), matrice priorità P0/P1, say/don't-say, domande+caveat, biblioteca fonti. Contenuto da **deep-research** (24 claim su 25, verifica avversariale a 3 voti); sorgente `.md` in `docs/Ferrovie/` (git-ignored). Aggiunto prop **`noindex`** al `BaseLayout` di trenitalia (mancava; ora come Eni/MIM). Palette FS carbonio/rosso-fs/ambra, classi `.dw-*`. (2) **Skill `experience-brief`**: nuova sezione **«Delivery — the brief becomes a live internal dossier, not a doc»** (il brief semina esperienza **+** dossier war-room interno; quando l'ask è un dossier il deliverable è sempre la pagina web). Rigenerate le copie pubblicate su showcase (`public/skill/experience-brief-SKILL.md` + `.zip`) e aggiornato il fineprint `#skill` (IT/EN). Build trenitalia (20 pagine) + showcase verdi; `noindex` verificato nell'HTML; dossier letto a 1920/1440. Memorie `fst-giampaolo-adobe-day`, `dossier-as-internal-web-page`.
- (segue) **Revisione 8 set (stessa giornata)**: su richiesta owner rimossi dal dossier «Referente dell'Adobe Day», la timeline carriera + cifre LinkedIn (~45 AMS, ~160 app, ~650 app/~4.500 server, tappe Ferservizi/Pirelli/UNIM/EDS), il caveat «mapping società→DBP» e la fonte LinkedIn; §03 ridotto a come pensa/parlargli/cosa lo accende, riscritti senza claim di carriera.

### Change log — UniCredit «workshop cut» + fix deck trasversali (2026-09-07, dal più recente) → dettaglio §5.6
- `e45d5e5` **fix(exp-design): roadmap home a blocchi uguali + `<title>` senza duplicato** — **2 regole generali** codificate in `CLAUDE.md` (sezione «Working rules»). (1) **Home roadmap = blocchi della stessa grandezza in griglia bilanciata**: 6 capitoli → **2 righe da 3** via `auto-rows-fr` + card `h-full` (era `md:grid-cols-4` → 4+2 disuguale su UniCredit); propagato a eni/agos/ferrari; esenti stepper maxmara + timeline trenitalia + pillars mim. (2) **`<title>` senza self-duplication**: `BaseLayout` faceva `{title} | <SiteName>` e la home passa `title`=nome sito → "SiteName | SiteName" (che inquina anche il link-preview, che legge `<title>`); guard `title === SiteName ? title : …` su **tutte** le layout + showcase (correggeva unicredit/maxmara/agos/trenitalia/eni). Build tutte OK; titoli home verificati nell'HTML buildato; roadmap verificate a video. Merge #3, deploiato + verificato live.
- `9c3bd24` **fix(decks): la freccia indietro non esce più dal deck verso l'hub** — root cause nel runtime di solution-gating (`BaseLayout.astro`): sulla prima/ultima sezione, senza sezione prev/next abilitata, il target era `homeFrom(curPrev|curNext)`; ma per la prima/ultima sezione quel valore è **già** la home dell'app, e `homeFrom` (rimuove l'ultimo segmento) ne toglieva uno di troppo → root dell'**hub** `/experience-design-factory/`. Si vede solo con `localStorage['edf-solutions-*']` valorizzato (tipico dopo l'Admin). Fix: `homeFrom()` deriva ora la home dal **path corrente** (`location.pathname` senza lo slug) → niente overshoot. Propagato alle 7 experience con questo pattern; **trenitalia era già corretto** (usa `b = BASE`). Merge #2, deploiato + verificato live (back su Scenario → cover, non hub).
- `ddb3650` **feat(unicredit): workshop cut — customer story a 6 capitoli + backdrop Firefly** — vedi **§5.6** per il dettaglio completo (6 capitoli in flow, single-persona Marco, MCA tenuto/B2B fuori, slide engine-map, 3 backdrop Firefly, sezioni funnel fuori-flow ma su disco). Merge #1. Memoria `unicredit-workshop-cut`.

### Change log — MIM «La voce del Ministero»: arricchimento + persona Giulia + imagery non-letterform + /trasformazione rimossa (2026-09-07, dal più recente) → dettaglio §28.6
- `af499f7` **rimossa la pagina test `/trasformazione`** (orfana dopo la nuova chiusura): cancellati `trasformazione.astro`, `keyframes.manifest.ts`, `video.manifest.ts`, i 4 keyframe `kf-transform-*`. Deck a **10 pagine** build. `scripts/build-video.ts` (tooling) resta. Chiude di fatto i vecchi P1 «rigenerare il video AI-slop» / «Firefly Video API enterprise» (non più necessari per MIM).
- `81d244e` **fonti con link diretti + journey difendibile (privacy) + claim misura grounded**: **ogni** riga-fonte ora ha un `<a>` diretto (`.alf-src a`/`.alf-stat-src a`) su tutte le slide; **journey di Giulia** ripensato per la privacy (via «Posizione GPS aggiornata» = push del punteggio → avvisi procedurali non sensibili che rimandano al canale ufficiale, «non un database di marketing»); claim assoluto «non ha mai misurato» → **«oggi non misura»** (persone) + «quasi mai misurate» (thesis); backdrop `soft-6` (era un libro aperto) rigenerato astratto.
- `3fd2d9a` **nuova sezione `/storia` (persona Giulia) + casi d'uso documentati + imagery non-letterform + backdrop per-slide + motion + chiusura**: 3 slide storia (ritratto/notte/evolve, journey card RT-CDP+AJO), `41.901 immissioni` + scala audience con fonti, **23 backdrop distinti** via `--bg-src` (manifest `backdrops.manifest.ts`) al posto di 3 ripetuti, drift+view-transition+stagger, slide-trust customer-facing, chiusura con reprise 3 verbi. `audit:deck` 0 HARD; typecheck 0; **UAT visiva completa** (chiude il P2 «UAT visiva MIM»). Memoria `mim-alfabeti-enrichment`; spec `…2026-09-04-mim-alfabeti-enrichment-design.md`.

