---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '@edf/core/layouts/Navigation.astro';
import DeckContainer from '@edf/core/blocks/immersive/DeckContainer.astro';
import Slide from '@edf/core/blocks/immersive/Slide.astro';
import CoverHero from '@edf/core/blocks/CoverHero.astro';
import SlideBackdrop from '@edf/core/blocks/SlideBackdrop.astro';
import MediaSlot from '@edf/core/blocks/MediaSlot.astro';
import { href } from '@edf/core/utils/url.js';
import { mediaProps } from '../../assets.index';

const base = import.meta.env.BASE_URL;
const currentPath = Astro.url.pathname;
const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Acquisizione', href: '/acquisizione' },
  { label: 'Engagement', href: '/engagement' },
  { label: 'Conversione', href: '/conversione' },
  { label: 'Loyalty', href: '/loyalty' },
  { label: 'Il Motore', href: '/motore-adobe' },
  { label: 'Persona', href: '/persona' },
];

// Slide-manifesto: i quattro momenti come archi di un'unica relazione.
const tappe = [
  { label: 'Acquisizione', essence: 'Incontrare' },
  { label: 'Engagement', essence: 'Conoscersi' },
  { label: 'Conversione', essence: 'Scegliere' },
  { label: 'Loyalty', essence: 'Restare' },
];
---
<BaseLayout title="Generazioni" description="Incontrare una nuova generazione, onorare chi sceglie Max Mara da sempre. Una relazione che attraversa il tempo.">
  <div class="fixed top-0 left-0 right-0 z-50">
    <Navigation items={navItems} currentPath={currentPath} base={base.replace(/\/$/, '')} />
  </div>

  <DeckContainer nextHref={href(base, '/acquisizione')}>

    <!-- ── SLIDE 0: Copertina ────────────────────────────────────── -->
    <Slide id="slide-cover" bg="secondary" align="center">
      <SlideBackdrop slot="backdrop" {...mediaProps('bg-linen')} imageClass="opacity-25" scrim="bg-[var(--surface-secondary)]/70" />
      <CoverHero
        size="hero"
        eyebrow="Max Mara × Adobe"
        title="Generazioni"
        lead="Incontrare una nuova generazione, onorare chi sceglie Max Mara da sempre. Una relazione che attraversa il tempo."
      />
    </Slide>

    <!-- ── SLIDE 1: Manifesto — l'intero percorso in una schermata ── -->
    <Slide id="slide-manifesto" bg="primary" align="center">
      <SlideBackdrop slot="backdrop" {...mediaProps('bg-cashmere')} imageClass="opacity-15" scrim="bg-[var(--surface-primary)]/82" />
      <p class="slide-eyebrow text-[var(--accent-primary)] mb-4" data-reveal>IL PERCORSO</p>
      <h2 class="slide-title text-[var(--ink-primary)] mb-5" style="font-size: clamp(2rem, 3.6vw, 3.4rem);" data-reveal data-reveal-delay="0.1">
        Una sola relazione, quattro momenti
      </h2>
      <p class="text-[var(--ink-secondary)] leading-relaxed max-w-2xl mx-auto mb-16" style="font-size: clamp(1.15rem, 1.5vw, 1.45rem);" data-reveal data-reveal-delay="0.2">
        Accogliere una nuova generazione e onorare chi sceglie Max Mara da sempre, dall'incontro alla fedeltà, lungo un solo filo che attraversa il tempo.
      </p>

      <!-- Il filo: da Giulia (la nuova) attraverso le quattro fasi, fino a Francesca (chi resta) -->
      <div class="relative w-full max-w-5xl mx-auto" data-reveal data-reveal-delay="0.3">
        <div class="hidden md:block absolute top-8 left-[10%] right-[10%] h-px bg-[var(--accent-primary)]/35" aria-hidden="true"></div>
        <div class="relative grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-y-8 gap-x-4 items-start">
          <div class="flex flex-col items-center gap-2 text-center">
            <div class="relative w-16 h-16 rounded-full overflow-hidden flex-shrink-0 bg-[var(--accent-primary)] ring-2 ring-[var(--surface-primary)] shadow-md">
              <MediaSlot {...mediaProps('persona-giulia')} fill noText="0,0,0,0" />
            </div>
            <span class="text-[clamp(0.95rem,1.1vw,1.15rem)] font-medium text-[var(--ink-primary)]">Giulia, 29</span>
            <span class="text-[0.8rem] text-[var(--ink-tertiary)]">la nuova cliente</span>
          </div>

          {tappe.map((t) => (
            <div class="flex flex-col items-center gap-2 text-center pt-3.5">
              <span class="w-4 h-4 rounded-full bg-[var(--accent-primary)] ring-4 ring-[var(--surface-primary)]" aria-hidden="true"></span>
              <span class="text-[clamp(0.95rem,1.1vw,1.15rem)] font-medium text-[var(--ink-primary)] mt-1.5">{t.label}</span>
              <span class="text-[0.8rem] text-[var(--ink-tertiary)]">{t.essence}</span>
            </div>
          ))}

          <div class="flex flex-col items-center gap-2 text-center">
            <div class="relative w-16 h-16 rounded-full overflow-hidden flex-shrink-0 bg-[var(--accent-secondary)] ring-2 ring-[var(--surface-primary)] shadow-md">
              <MediaSlot {...mediaProps('francesca-volto')} fill noText="0,0,0,0" />
            </div>
            <span class="text-[clamp(0.95rem,1.1vw,1.15rem)] font-medium text-[var(--ink-primary)]">Francesca, 52</span>
            <span class="text-[0.8rem] text-[var(--ink-tertiary)]">la cliente storica</span>
          </div>
        </div>
      </div>
    </Slide>

    <!-- ── SLIDE 2: Invito — il valore + entra nel percorso ──────── -->
    <Slide id="slide-payoff" bg="brand" align="center">
      <SlideBackdrop slot="backdrop" {...mediaProps('bg-cashmere')} imageClass="opacity-20" scrim="bg-[var(--accent-primary)]/78" />
      <p data-display class="font-[family-name:var(--font-display)] font-light leading-none text-[var(--ink-primary)] mb-6" style="font-size: clamp(4.5rem, 12vw, 11rem);">
        <span data-count-up="2.4">0</span>
      </p>
      <p class="text-[clamp(1.5rem,2.4vw,2.25rem)] font-[family-name:var(--font-display)] font-normal text-[var(--ink-primary)] leading-snug mb-5 max-w-3xl" data-reveal data-reveal-delay="0.2">
        Bastano 2.4 esperienze negative per perdere una cliente. Ne servono 6.7 positive per conquistarla.
      </p>
      <p class="text-[clamp(1.05rem,1.3vw,1.25rem)] text-[var(--ink-primary)]/70 mb-12 max-w-2xl" data-reveal data-reveal-delay="0.28">
        Fonti: McKinsey · Deloitte · Adobe &amp; Incisiv, ricerca sulla loyalty nel retail, 2026
      </p>

      <div data-reveal data-reveal-delay="0.5">
        <a
          href={href(base, '/acquisizione')}
          class="inline-flex items-center gap-3 px-10 py-5 text-[clamp(1.05rem,1.3vw,1.3rem)] font-medium tracking-wide bg-[var(--ink-primary)] text-[var(--ink-inverse)] hover:bg-[var(--surface-inverse)] transition-colors duration-300 focus-visible:outline-2 focus-visible:outline-[var(--ink-primary)] focus-visible:outline-offset-4"
        >
          Inizia il percorso · Acquisizione
          <svg width="20" height="20" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </Slide>

  </DeckContainer>
</BaseLayout>
