# Pexels API key — free at https://www.pexels.com/api/
# Used ONLY at build-time locally by `pnpm assets:build`.
# It is never exposed to the static site or to CI. Keep your real .env out of git.
PEXELS_API_KEY=
